# Copy this file, rename the copy to .env and replace MONGO_URI with our admin login details (#info channel in the discord)
# We use this to prevent exposing our actual credentials when we make the repo public
PORT=5000
MONGO_URI=...
