/**
 * This file contains the API routes for querying the admin API
 */

const express = require("express");
const {} = require("../controllers/admin");
const router = express.Router();

module.exports = router;
